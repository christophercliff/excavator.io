## Build

Clean rebuild:

```
$ npm run build
```

Dirty rebuild with a watch script and local server:

```
$ npm run dev
```

## Deploy

Do a clean rebuild and force push to `master`:

```
$ npm run deploy
```
